//! `hackline-agent` binary entry point. Argv parsing and the logging
//! subscriber install live here; everything else is a library function.

mod config;
mod connect;
mod error;
mod info;
mod liveliness;

fn main() {
    unimplemented!("hackline-agent: scaffolding only");
}
