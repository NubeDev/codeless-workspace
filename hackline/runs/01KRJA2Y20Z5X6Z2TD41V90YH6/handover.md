## Done

- listed contents of crates/ at repo root: five subdirectories (hackline-agent, hackline-cli, hackline-core, hackline-gateway, hackline-proto)

## Next

- (none)

## What you need to know

- The crates/ directory at the workspace root contains Rust crates prefixed `hackline-*`, not the `codeless-*` names referenced in CLAUDE.md / SCOPE.md. Worth flagging if the next session expects codeless-named crates.
- No nested crates/ directories exist elsewhere in the worktree.

## Open questions

- Are the `hackline-*` crates the current naming, or pre-rename leftovers vs. the `codeless-*` names in DOCS/SCOPE.md?
